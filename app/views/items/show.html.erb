<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>
  <p><%#= @item.name%></p>
  <p><%#= @item.description %></p>
  <%# if current_user %>
    <p><%#= link_to 'Back' , profile_path(current_user.id) %> </p>
  <%# else %>
    <%#= link_to image_tag(@item.image.url(:large)), @item.image.url %>
  <%# end %>

<%= link_to 'Back' , profile_path(current_user.id) %>

<div class="row text-center">
  <div class="col-md-12 product-info-wrapper">
    <p><%= @item.name%></p>
    <p><%= @item.description %></p>
  </div>
</div>

<div class ="col-md-offset-3">
  <%= image_tag(@item.image.url, :size => "200%x200%") %>
</div>


 <%= render 'show' %>

</body>
</html>
